/** Matches API `UserRole` for workspace and layout guards. */
export const DEV_ROLE = "dev";

export const IT_STAFF_ROLES = ["dev", "it", "admin", "head_it"];

export const ADMIN_NAV_ROLES = ["dev", "admin", "head_it"];

export const DEV_ONLY_ROLES = ["dev"];
